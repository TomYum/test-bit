framework:
  secret: S0ME_SECRET

parameters:
  redis.host: "127.0.0.1"
  redis.port: "6379"
  redis.auth: "secret"

  redis.stat.primary_key: "country_stat"

services:
  redis:
    class: Redis
    calls:
      - [ "connect",["%redis.host%", "%redis.port%"]]
      - ["auth",["%redis.auth%"]]


